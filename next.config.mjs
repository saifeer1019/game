/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['f.woowoowoowoo.net', 'lewdflix.com', 'owo.lewd.ninja', 'new.lewd.ninja', 'attachments.f95zone.to'],
  },
};
 
export default nextConfig;
